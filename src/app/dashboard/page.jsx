import { DashboardPage } from '@/views/dashboard';

export const metadata = {
  title: 'Dashboard - Zentra',
  description: 'Painel de controle da sua produtividade no Zentra',
};

export default function Dashboard() {
  return <DashboardPage />;
}
